package noapplet.example;

import java.awt.*;

public class Moon extends Planet {
    Moon(CelestialBody parent, Color color, double size, double angle) {
        super(parent, color, size, angle);
    }
}
